---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class="flex flex-col" id={title.toLocaleLowerCase().replaceAll(" ", "-")}>
  <h3 class="text-center mb-6">{title}</h3>
  <slot />
</div>
